name = "ficha-worker"
main = "src/index.ts"
compatibility_date = "2024-05-01"

# Secrets (set via CLI, never in this file):
#   wrangler secret put DATABASE_URL
#   wrangler secret put API_KEY

[[ratelimits]]
name = "FICHA_RATE_LIMITER"
namespace_id = "1001"

  [ratelimits.simple]
  limit = 100
  period = 60
